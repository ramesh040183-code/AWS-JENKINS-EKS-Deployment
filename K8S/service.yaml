apiVersion: v1
kind: Service

metadata:
  name: foodweb1-service

spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      
  selector:
    app: foodweb1-app

  type: LoadBalancer

  